# Wallet account menu UI refresh

## Goal

Improve the connected wallet account modal on `http://localhost:3000/profile` so it has a cleaner, Material UI-inspired dialog layout and better button hierarchy.

## Scope

- Update the wallet account modal component.
- Keep existing wallet copy, profile, logout, and close behavior.
- Do not add fallback behavior or mock paths.

## Validation

- Run frontend lint or build checks where available.
- Verify the profile page modal visually in the browser when feasible.
