# NFT 项目中期扫描报告与答辩讲稿

## 一、项目扫描后的总体判断

### 1. 项目定位

从代码实现来看，这个项目不是单纯的 NFT 页面展示，而是一个相对完整的 `NFT 数字藏品市场系统`。它已经具备了以下几条核心能力链路：

1. 用户注册、登录与个人资料管理
2. 钱包连接、钱包签名登录
3. 媒体文件上传与头像上传
4. 基于 Pinata 的 IPFS 文件与元数据上链前准备
5. 基于 Solidity 合约的 NFT 铸造、上架、购买
6. 后端对 NFT、订单、用户信息的链下持久化
7. 市场浏览、详情页购买、个人中心查看持有与交易历史

也就是说，这个项目目前已经不是“概念验证 Demo”，而是一个已经形成业务闭环雏形的中期系统。

如果按毕业设计或课程设计的中期标准来评估，我认为这个项目当前已经完成了 `核心主流程的 70% 左右`，其中“功能闭环”完成度较高，但“工程化、鲁棒性、安全性和部署稳定性”还有继续打磨空间。

### 2. 仓库结构与技术栈

当前仓库主要分为四部分：

1. `backend`
   Go 后端，负责用户、NFT、订单、上传、IPFS 接口、JWT 鉴权、数据库持久化。
2. `frontend`
   Next.js 前端，负责页面展示、交互逻辑、钱包接入、链上交易触发和用户体验。
3. `contracts`
   Solidity 智能合约源码，定义 NFT 铸造、上架和购买逻辑。
4. `hardhat`
   本地链上开发与部署环境，用于合约编译和部署到 Sepolia 测试网。

技术选型大致如下：

1. 前端：`Next.js 14 + React 18 + Tailwind CSS + Axios + Ethers.js`
2. 后端：`Go 1.22 + Gorilla Mux + GORM + MySQL + JWT + bcrypt`
3. 区块链：`Solidity 0.8.24 + OpenZeppelin ERC721URIStorage + Hardhat`
4. 存储：`MySQL` 负责链下业务数据，`Pinata/IPFS` 负责媒体文件与元数据存储
5. 钱包交互：浏览器注入钱包，兼容 `MetaMask / OKX / Bitget`

### 3. 当前系统的真实架构

这个项目采用的是一个典型的 `链上 + 链下协同` 架构，而不是所有内容都直接写到链上。

#### 链上部分负责

1. NFT 的唯一性确权
2. NFT 铸造
3. NFT 上架价格记录
4. NFT 原生代币购买
5. 所有权转移

#### 链下部分负责

1. 用户账户体系
2. 钱包登录会话管理
3. NFT 详情数据聚合展示
4. 订单记录查询
5. 头像和资料维护
6. 市场筛选、分类、收藏、个人中心等业务体验

这种设计的优点是：

1. 既保留了区块链的确权和交易可信性
2. 又避免把所有业务都压到链上，降低成本
3. 页面响应、数据筛选和用户体验会更好

这也是一个比较符合实际产品思路的架构。

## 二、代码扫描得到的模块化结论

### 1. 后端模块分析

后端入口在 `backend/cmd/server/main.go`，启动 HTTP 服务并挂载 CORS 中间件。

后端路由集中定义在 `backend/internal/router/router.go`，可以看出后端已经形成比较清晰的模块边界，主要 API 分类包括：

1. `auth` 认证模块
2. `nfts` NFT 资源模块
3. `orders` 订单模块
4. `upload` 上传模块
5. `ipfs` IPFS 模块

从路由设计上看，已经具备 REST 风格接口思路。

#### 认证模块

认证模块包含两套逻辑：

1. 邮箱密码注册登录
2. 钱包 nonce 签名登录

其中：

1. 注册时使用 `bcrypt` 对密码做哈希处理
2. 登录成功后发放 `JWT`
3. 钱包登录时，后端先为钱包生成随机 `nonce`
4. 前端拿到 `nonce` 后调用钱包执行 `personal_sign`
5. 后端通过签名恢复公钥地址，校验钱包持有权
6. 校验成功后再签发 JWT

这说明项目已经把传统 Web 认证和 Web3 认证结合在一起，具备一定完整性。

#### NFT 模块

NFT 模块负责：

1. NFT 列表查询
2. NFT 详情查询
3. NFT 创建
4. NFT 上架价格更新

链下 NFT 模型中保存了：

1. 合约地址
2. Token ID
3. 名称与描述
4. 图片地址、媒体地址、媒体类型、MIME 类型
5. TokenURI、MetadataURL
6. 所属分类
7. 所有者 ID
8. 价格与价格单位

这意味着它不是简单只存一张图，而是已经支持了音频、视频、图片等不同数字藏品形态。

#### 订单模块

订单模块负责：

1. 创建订单
2. 查询我卖出的订单
3. 查询我买入的订单
4. 查询某个 NFT 的历史成交记录

后端在创建订单时会同步修改 NFT 的拥有者，并将价格重置为 0，说明链下系统已经开始承担“交易结果展示层”的角色。

#### 上传与 IPFS 模块

这个部分是项目非常关键的亮点。

项目不是直接把图片地址写死，而是实现了：

1. 普通头像上传到本地静态目录
2. NFT 主文件上传到 Pinata
3. 音频和视频可选上传封面图
4. 后端自动生成 NFT 元数据 JSON
5. 把元数据再上传到 IPFS
6. 最终返回 `assetUri`、`metadataUri`、`gatewayUrl` 等信息供前端继续铸造

同时后端还做了分类和媒体类型校验，例如：

1. 艺术类只允许图片
2. 音乐类只允许音频
3. 视频类只允许视频

这一点说明项目在业务约束层面也考虑得比较完整。

### 2. 前端模块分析

前端使用的是 `Next.js App Router` 结构，主要页面包括：

1. 首页
2. 登录页
3. 注册页
4. NFT 市场页
5. NFT 创建页
6. NFT 详情页
7. 个人中心页

#### 首页

首页并不是静态介绍页，而是会真实请求后端 NFT 列表，并展示：

1. 在售数量
2. 地板价
3. 平均价
4. 热门分类
5. 精选 NFT

这说明首页已经开始承担市场概览作用。

#### 市场页

市场页功能较完整，支持：

1. 分类筛选
2. 名称/描述/Token 检索
3. 价格区间筛选
4. 多种排序方式
5. 收藏夹本地缓存

这类功能虽然不属于区块链底层逻辑，但对于“一个真正能使用的 NFT 市场”非常重要。

#### 创建页

创建页是当前系统最复杂、最能体现项目工作量的页面之一。

这个页面实现了完整的创建流程：

1. 输入 NFT 名称、描述、分类和价格
2. 上传图片、音频或视频
3. 针对音频和视频补充封面
4. 前端先进行类型和大小校验
5. 先把主文件和元数据上传到 IPFS
6. 再调用钱包执行链上铸造
7. 如有价格则直接执行“铸造并上架”
8. 链上成功后，再把结果同步到后端数据库
9. 页面中还设计了分阶段进度卡片，向用户展示当前在 IPFS、钱包、链上确认、后台同步的哪一步

这说明项目已经实现了从“文件准备”到“链上交易”再到“链下同步”的完整串联。

#### NFT 详情页

详情页实现了：

1. 展示图片、音频、视频等不同媒体形式
2. 展示 NFT 名称、描述、价格、分类、TokenID、TokenURI
3. 非所有者可购买
4. 所有者可修改价格或重新上架
5. 展示成交历史和交易哈希
6. 购买与上架也都带有交易进度反馈

从产品角度看，详情页已经不仅仅是展示页面，而是交易页面。

#### 个人中心页

个人中心实现了：

1. 当前持有 NFT
2. 历史买入记录
3. 历史卖出记录
4. 用户名修改
5. 钱包地址与头像展示

这个页面使整个项目具备“用户资产管理”的基本能力。

### 3. 智能合约分析

智能合约 `NFTCollection.sol` 当前实现了一个简化版 ERC721 交易系统，核心函数有：

1. `safeMint`
2. `mintAndList`
3. `listToken`
4. `buy`
5. `getListing`
6. `totalMinted`

这份合约设计比较适合中期阶段，因为它没有上来就做拍卖、版税、多人竞价等复杂逻辑，而是优先完成最基本的业务闭环：

1. 铸造
2. 上架
3. 购买
4. 所有权转移
5. 支付结算

这是一种非常合理的迭代策略。

## 三、项目目前已经形成的核心业务闭环

### 1. 用户注册与登录闭环

 Web3 方式：钱包连接 + nonce 签名

这说明项目是兼顾普通用户和区块链用户使用习惯的。

### 2. NFT 创建闭环

NFT 创建已经形成以下完整链路：

1. 用户进入创建页
2. 填写 NFT 信息
3. 上传主文件
4. 可选上传音视频封面
5. 后端把资源上传到 IPFS
6. 自动生成元数据
7. 前端调用合约执行铸造
8. 若设置价格则同时上架
9. 前端把最终结果写回后端
10. 用户可在市场和详情页立即看到该 NFT

这条链路是整个项目最重要的里程碑。

### 3. NFT 购买闭环

购买流程也已经串起来了：

1. 买家进入详情页
2. 点击购买
3. 钱包弹出交易确认
4. 链上执行 `buy`
5. 前端拿到交易哈希
6. 请求后端创建订单
7. 后端更新 NFT 所有者和订单状态
8. 详情页与个人中心展示新的持有关系和历史记录

这说明系统已经不只是“发 NFT”，而是具备了“交易 NFT”的能力。

### 4. 个人资产管理闭环

用户在个人中心可以看到：

1. 当前持有的 NFT
2. 已买入的订单
3. 已卖出的订单
4. 个人资料

这让项目从“单个功能页面”升级成了“一个完整的 NFT 小型平台”。

## 四、项目的主要亮点

### 1. 不是单页 Demo，而是全链路系统

很多 NFT 课程项目停留在：

1. 写一个 ERC721 合约
2. 做一个简单 mint 页面
3. 展示几张图片

但你的项目已经做到了：

1. 前端
2. 后端
3. 数据库
4. IPFS
5. 钱包登录
6. 交易历史
7. 个人中心

这是一个明显更完整的系统化设计。

### 2. 支持多媒体 NFT，而不是只支持图片

系统支持：

1. 图片
2. 音频
3. 视频

而且音频、视频支持封面，这一点很适合拿出来作为答辩亮点，因为它体现了你对 NFT 形态不是停留在“图片头像”的理解。

### 3. 采用链上链下分层架构

这个架构选择是有技术判断的：

1. 链上负责可信交易与所有权
2. 链下负责高频查询和用户体验

这说明你考虑过真实应用落地，而不是把所有问题都简单丢给链上。

### 4. 钱包登录设计较完整

项目不是直接读取钱包地址当登录成功，而是采用：

1. 后端生成 nonce
2. 钱包签名
3. 后端恢复签名地址
4. 再发 JWT

这是一种更标准的 Web3 登录流程。

### 5. 用户体验考虑比较细

前端加入了：

1. 创建进度提示
2. 购买进度提示
3. 分类标签
4. 收藏夹
5. 多种排序和过滤
6. 响应式布局
7. 视觉统一的市场卡片样式

这类细节会让老师明显感受到项目不是拼凑的，而是经过持续迭代的。

## 五、当前还存在的不足与中期阶段问题

这一部分非常重要。中期答辩不怕有问题，怕的是你不知道自己有什么问题。下面这些点反而会让你的汇报更真实、更专业。

### 1. 前端生产构建还不够稳定

我对前端执行了 `npm run build`，结果是：

1. 编译阶段可以通过
2. 但在收集页面数据时出现 `.next/server/pages-manifest.json` 缺失错误

这说明当前前端在开发环境很可能能跑，但生产构建仍需进一步收口和排查。

中期答辩时可以表述为：

“当前核心交互和功能链路已经跑通，下一阶段重点之一是继续完善项目的生产构建与部署稳定性。”

### 2. 链上结果与链下同步目前主要依赖前端回传

例如：

1. NFT 创建后，前端把 `tokenId`、`tokenUri`、`metadataUrl` 等结果提交给后端
2. 购买成功后，前端把 `txHash` 提交给后端创建订单

这意味着当前系统已经完成了业务闭环，但链下数据对链上结果的校验还不够严格。

下一步更理想的做法是：

1. 后端主动校验链上交易回执
2. 或通过事件监听器同步 Mint、Listed、Purchased 事件

这会让系统的数据一致性更强。

### 3. 账户体系还没有完全打通

当前系统同时存在：

1. 邮箱密码用户
2. 钱包签名用户

但是“邮箱账户”和“钱包账户”的绑定关系还没有形成统一设计。也就是说，这两套身份体系目前更像并行存在，而不是完全融合。

这属于中期阶段非常正常的问题，可以作为后续优化重点。

### 4. 自动化测试不足

后端 `go test ./...` 可以通过，但实际上没有测试用例。

也就是说：

1. 项目代码可以编译
2. 但还缺少单元测试、接口测试和合约测试

如果老师问测试，你可以坦诚回答：

“目前重点先完成主业务链路，下一阶段会补充核心路径测试，包括登录、创建、购买和订单同步等流程。”

### 5. 工程配置仍偏开发态

从后端配置和中间件来看，目前仍有一些开发阶段特征：

1. CORS 放得比较宽
2. 默认 JWT Secret 和数据库配置仍带有开发默认值
3. 部署配置和环境隔离可以继续加强

这一点不需要主动说成“漏洞”，而是说成：

“当前系统优先保障功能验证，后续会继续加强生产部署安全配置。”

### 6. 部分中文文案存在编码显示异常

在若干页面和后端错误信息里，可以观察到中文字符串有乱码痕迹。这对系统功能影响不大，但会影响展示质量和答辩演示观感。

这个点很适合列为一个“易修复但提升明显”的后续优化项。

## 六、可用于答辩的完成度总结

如果你需要一句比较稳妥的总结，我建议你这样说：

“截至中期，我已经完成了项目的总体架构设计、前后端主要模块开发，以及 NFT 创建、IPFS 存储、链上铸造、市场展示、链上购买、订单同步和个人中心等核心功能，项目已经具备从用户登录到 NFT 交易的基本闭环。当前还处于从功能可用向工程稳定和系统完善过渡的阶段，后续重点会放在账号体系统一、链上事件校验、构建部署稳定性和自动化测试补全上。”

## 七、中期答辩详细讲稿

下面这份稿子是可以直接口述的版本，你答辩时可以按 8 到 12 分钟控制节奏。

---

### 中期答辩讲稿正文

各位老师好，我的项目题目是《基于区块链的 NFT 数字藏品市场系统设计与实现》。

我这次中期汇报主要从五个方面展开，分别是项目背景与目标、系统总体设计、目前已经完成的核心功能、项目中的关键技术点，以及下一阶段的工作计划。

首先介绍一下项目背景。

我选择 NFT 方向，主要是因为 NFT 结合了区块链的确权能力和数字内容的资产化能力。传统数字内容，比如图片、音乐、视频，在互联网上虽然很容易传播，但所有权和唯一性很难证明。而 NFT 正好可以通过链上 Token 的形式，对数字内容进行唯一标识和交易记录，从而实现数字资产确权、流转和追溯。

但是如果只是单纯写一个 ERC721 合约，其实还不能称为一个完整系统。真实的 NFT 平台还需要有前端页面、用户体系、媒体资源存储、元数据管理、市场展示、交易记录，以及用户资产管理等功能。所以我的项目目标并不是只做一个“能铸造 NFT 的页面”，而是实现一个包含前端、后端、数据库、IPFS 和智能合约在内的完整 NFT 数字藏品市场原型系统。

接下来介绍系统总体设计。

我的项目采用的是“链上加链下协同”的架构。链上部分主要负责 NFT 的铸造、上架、购买和所有权转移，也就是负责资产确权和交易可信性。链下部分主要负责用户登录、资料管理、市场查询、交易历史展示和媒体资源聚合等业务逻辑。这样设计的原因是，如果把所有信息都直接放到链上，不仅成本高，而且查询效率和用户体验都会比较差；而将高频业务数据放在链下数据库中管理，可以显著提升系统的实用性。

从项目结构上看，整个系统分为四部分。

第一部分是前端，我使用的是 Next.js 和 React 来构建用户界面。前端主要负责市场展示、创建 NFT、详情页交易、个人中心，以及钱包连接和链上交易交互。

第二部分是后端，我使用 Go 语言来实现接口服务。后端负责用户管理、JWT 鉴权、NFT 数据管理、订单管理、头像上传和 IPFS 上传等功能，同时通过 GORM 对 MySQL 数据库进行操作。

第三部分是智能合约部分，我使用 Solidity 编写了 NFT 合约，并基于 OpenZeppelin 的 ERC721URIStorage 进行扩展，实现了铸造、上架、购买等功能。

第四部分是 Hardhat 开发环境，用于合约编译、部署以及连接 Sepolia 测试网。

下面重点介绍目前已经完成的核心功能。

第一个完成的模块是用户认证模块。

钱包签名登录。具体流程是后端先为指定钱包地址生成一个随机 nonce，前端拿到 nonce 之后调用钱包进行签名，后端再根据签名恢复钱包地址并完成验证，验证通过后签发 JWT。这样可以避免只凭钱包地址就直接登录，安全性更高。

第二个完成的模块是 NFT 创建模块，这也是我目前投入工作量最大的部分。

在创建页面中，用户可以输入 NFT 的名称、描述、分类和价格，并上传媒体文件。系统支持图片、音频和视频三种类型。如果上传的是音频或视频，还可以额外上传封面图。前端会先做文件类型和大小校验，然后调用后端 IPFS 接口把主文件和元数据上传到 Pinata。后端会根据媒体类型自动生成符合 NFT 使用习惯的元数据 JSON，例如图片类会生成 image 字段，音频和视频类会使用 animation_url，同时保留封面信息。上传成功后，前端再调起钱包执行合约中的铸造逻辑。如果用户设置了价格，系统会直接调用“铸造并上架”函数；如果没有设置价格，就只执行铸造。链上交易确认完成之后，前端再把合约地址、TokenID、TokenURI、媒体地址和价格等信息同步到后端数据库中，形成链下可查询的 NFT 记录。

这条流程打通以后，项目就实现了从“文件上传”到“链上铸造”再到“后端入库”的完整闭环。

第三个完成的模块是 NFT 市场浏览模块。

首页会从后端读取在售 NFT，并显示市场概览信息，包括在售数量、地板价、平均价和热门分类。市场页则支持更丰富的功能，包括按类别筛选、关键词检索、价格区间筛选和不同排序方式。同时我还实现了本地收藏夹功能，用户可以把感兴趣的 NFT 暂存在浏览器本地存储中。这样做的意义在于，项目不仅实现了后端接口，也开始具备真实产品的交互体验。

第四个完成的模块是 NFT 详情页和购买流程。

详情页会根据 NFT 的媒体类型自动适配展示方式。图片直接展示，音频会显示封面并嵌入播放器，视频则支持视频播放。页面中可以展示 NFT 的名称、描述、价格、分类、链上编号、TokenURI 和拥有者信息。对于普通用户，如果当前 NFT 处于上架状态，就可以直接点击购买。购买时前端会调用合约中的 buy 函数，通过钱包完成链上支付与所有权转移。链上交易确认以后，前端会把交易哈希提交给后端创建订单，后端再更新 NFT 所有者和订单状态。这样最终用户不仅完成了链上购买，而且还能在系统里看到订单记录和持有状态变化。

第五个完成的模块是个人中心模块。

个人中心页面目前已经支持查看当前持有的 NFT、历史买入记录和历史卖出记录，同时也支持修改用户名和展示钱包地址、头像信息。这个模块的意义在于，它把分散的 NFT、订单和用户信息汇总到了一个统一的资产管理界面中，使整个系统从“功能点集合”变成了“完整平台原型”。

下面介绍本项目中的几个关键技术点。

第一个技术点是 Web2 与 Web3 认证的结合。

在这个项目中，我没有只保留单一登录模式，而是同时支持邮箱密码登录和钱包签名登录。钱包登录采用的是 nonce 加签名的方式，这是一种比较标准的 Web3 身份验证思路。这样做的好处是，既能服务普通用户，也能服务链上用户，为后续账户打通和钱包绑定打下基础。

第二个技术点是 IPFS 元数据生成与多媒体支持。

很多简单 NFT 项目只上传图片，而我的系统在后端对不同媒体做了区分处理。系统可以识别 image、audio 和 video，并根据不同媒体类型组织元数据结构。例如，图片直接作为 image 字段，音视频则放在 animation_url 中，同时使用封面图作为展示图。这样做的结果是，系统能支持更丰富的数字藏品类型，而不局限于静态图片。

第三个技术点是链上链下数据协同。

我在设计时没有让前端只停留在调用合约，而是让后端承担了用户、NFT 和订单的链下业务管理职责。链上负责交易和确权，链下负责展示和查询。这种分层设计更接近真实平台，也能提高扩展性。例如后续如果要增加推荐、排行榜、后台管理或订单分析，都可以优先在链下完成。

第四个技术点是交互流程可视化。

我在创建 NFT 和购买 NFT 的页面中增加了进度卡片，把整个流程拆成 IPFS 上传、钱包确认、链上确认、后台同步和完成几个阶段。这样做虽然是前端体验层面的优化，但对于区块链应用非常重要，因为区块链交互往往存在等待时间长、用户不确定当前状态的问题。通过阶段化展示，用户可以清楚知道自己当前卡在哪一步。

接下来我总结一下目前的阶段性成果。

截至中期，我认为本项目已经完成了系统总体架构设计和核心主流程开发。具体来说，已经实现了用户认证、钱包登录、文件上传、IPFS 存储、NFT 创建、链上铸造、市场浏览、详情页交易、订单记录和个人中心等功能。也就是说，这个系统已经具备了从登录到创建、从展示到交易的基本业务闭环。

当然，中期阶段也还有一些没有完全收尾的部分。

第一，前端目前在生产构建时还存在稳定性问题，也就是开发功能已经跑通，但生产构建和部署环节还需要继续排查和优化。

第二，目前链上交易结果同步到后端，主要还是由前端把交易哈希和结果回传给后端完成记录，下一阶段我计划进一步加入链上交易回执校验，或者通过事件监听来提高链上链下数据一致性。

第三，目前邮箱账户和钱包账户都已经具备，但这两套身份体系之间还没有完全做统一绑定，下一阶段会进一步优化账户体系设计。

第四，当前系统缺少自动化测试。虽然主要代码已经可以编译，并且核心功能逻辑已经成型，但后续我会补充接口测试、核心业务测试和合约测试，增强项目的可靠性。

最后介绍一下下一阶段的计划。

下一阶段我主要会从四个方向继续推进。

第一，继续完善工程化能力，解决前端生产构建问题，优化部署流程，让系统从“开发可运行”走向“部署可交付”。

第二，完善链上链下同步机制，增加对交易回执和链上事件的校验，减少对前端回传结果的依赖。

第三，进一步统一账户体系，打通邮箱用户和钱包用户的关系，提升身份一致性。

第四，补充测试和异常处理，包括登录、创建、购买、订单同步等关键路径，提升系统的稳定性和可维护性。

总的来说，我这个阶段的重点是先把 NFT 市场系统的核心业务闭环真正跑通。目前这个目标已经基本完成。后续我会继续围绕系统稳定性、数据一致性和工程完善度做深入优化，争取在最终答辩前把项目提升为一个更完整、更可靠的 NFT 平台原型。

我的汇报完毕，谢谢各位老师。

---

## 八、如果老师追问，你可以这样答

### 1. 老师问：你这个项目的创新点是什么？

你可以回答：

“我的项目创新点不在于单个算法，而在于把 NFT 的完整业务闭环做了出来。相比只做 ERC721 铸造 Demo，我把钱包登录、IPFS 元数据、多媒体 NFT、链上购买、链下订单同步和个人资产管理整合到了同一个系统中。尤其是音频、视频 NFT 和链上链下协同这一点，是我项目比较突出的地方。”

### 2. 老师问：为什么要做链下数据库？

你可以回答：

“因为链上适合做确权和交易，但不适合做高频业务查询。像用户资料、订单列表、市场筛选、个人中心这些功能，如果完全依赖链上，不仅成本高，而且体验差。所以我采用了链上负责可信交易，链下负责业务展示和聚合查询的架构。”

### 3. 老师问：你这个系统现在最大的不足是什么？

你可以回答：

“我认为当前最大的不足有两个。第一是生产构建和部署稳定性还需要继续优化；第二是链上交易结果到链下数据库的同步，目前更多依赖前端回传，下一阶段我会继续增加链上回执校验和事件监听，提高一致性和可靠性。”

### 4. 老师问：为什么中期不先做测试？

你可以回答：

“因为这个阶段我的主要任务是先把主功能链路打通，包括上传、IPFS、铸造、购买、订单同步这些核心流程。等主流程稳定之后，再补充自动化测试会更高效。下一阶段测试会作为重点补全内容。”

### 5. 老师问：你这个项目现在算完成到什么程度？

你可以回答：

“如果按系统核心能力来看，我认为已经完成 70% 左右。主业务闭环已经形成，但在工程稳定性、账户统一、数据校验和测试方面还有明显的完善空间。”

## 九、答辩时的表达建议

### 1. 要主动强调“闭环”

你不要只讲“我写了哪些页面”，要不断强调：

1. 登录闭环
2. 创建闭环
3. 购买闭环
4. 资产管理闭环

老师更容易认可“系统性”。

### 2. 不要把问题说成失败，要说成阶段性优化方向

例如不要说：

“我这个地方还不行。”

更好的说法是：

“当前已经完成核心功能验证，下一阶段会重点完善这一部分的工程稳定性和一致性。”

### 3. 讲功能时，一定带上技术实现

例如不要只说：

“我实现了钱包登录。”

最好说：

“我实现了基于 nonce 和 personal_sign 的钱包签名登录，后端会恢复签名地址并签发 JWT。”

这样老师会感觉你是真的理解，而不是只会调库。

## 十、一句话总结版

如果最后老师让你简短总结，你可以这样收尾：

“我的项目目前已经完成了 NFT 数字藏品市场系统的核心闭环，包括用户认证、钱包登录、IPFS 存储、链上铸造、市场展示、链上购买、订单同步和个人中心管理。中期阶段重点任务已经基本完成，后续将继续提升系统的工程稳定性、链上链下一致性和测试完备度。” 

