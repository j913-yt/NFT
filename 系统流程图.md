# NFT 项目系统流程图

说明：以下流程图依据 `D:\AStudy\NFT` 当前项目实现绘制，适合作为论文中的“系统总体流程图”或“系统业务流程图”。

## 1. 系统总体流程图

```mermaid
flowchart TD
    A([用户访问平台]) --> B[进入前端首页/市场页]
    B --> C{是否已完成钱包登录}

    C -- 否 --> D[选择钱包插件<br/>MetaMask / OKX / Bitget]
    D --> E[前端请求后端生成 nonce]
    E --> F[用户在钱包中签名]
    F --> G[后端校验签名并签发 JWT]
    G --> H[前端保存 token 和用户信息]
    H --> I[进入系统功能页面]

    C -- 是 --> I[进入系统功能页面]

    I --> J{用户选择的功能}

    J -- 创建 NFT --> K[填写名称/描述/分类/价格/版税]
    K --> L[上传图片/音频/视频文件]
    L --> M[后端校验文件类型与大小]
    M --> N[后端上传主文件/封面到 IPFS]
    N --> O[后端生成并上传 Metadata]
    O --> P[前端调用智能合约执行铸造]
    P --> Q{是否设置价格}
    Q -- 是 --> R[调用 mintAndList<br/>铸造并上架]
    Q -- 否 --> S[调用 safeMint<br/>仅铸造不上架]
    R --> T[前端将 NFT 数据同步到后端数据库]
    S --> T
    T --> U[市场页与个人中心显示新 NFT]

    J -- 浏览/购买 NFT --> V[市场页筛选/搜索 NFT]
    V --> W[进入 NFT 详情页]
    W --> X{当前用户是否为拥有者}

    X -- 否 --> Y[点击购买]
    Y --> Z[前端调用 buy 函数]
    Z --> AA[链上完成支付、转账与所有权转移]
    AA --> AB[前端提交 txHash 到后端]
    AB --> AC[后端创建订单并更新 NFT owner]
    AC --> AD[详情页/个人中心更新买入卖出记录]

    X -- 是 --> AE[修改价格 / 重新上架 / 下架]
    AE --> AF[前端调用 listToken 或 cancelListing]
    AF --> AG[链上更新上架状态]
    AG --> AH[后端同步 price 和 owner 数据]
    AH --> AI[市场页与详情页刷新展示]

    J -- 个人中心 --> AJ[查看当前持有 NFT]
    AJ --> AK[查看历史买入/卖出订单]
    AK --> AL[修改用户名或上传头像]
    AL --> AM[后端更新用户资料]
```

## 2. 精简版论文插图流程图

如果你想放到论文里，这个精简版更适合直接当“图 C-1 系统总体流程图”使用。

```mermaid
flowchart TD
    A([开始]) --> B[用户进入 NFT 平台]
    B --> C{是否已登录}
    C -- 否 --> D[钱包连接与签名登录]
    D --> E[后端签发 JWT]
    E --> F[进入系统]
    C -- 是 --> F[进入系统]

    F --> G{选择业务功能}
    G --> H[创建 NFT]
    G --> I[浏览市场]
    G --> J[个人中心]

    H --> H1[上传媒体文件]
    H1 --> H2[IPFS 存储与 Metadata 生成]
    H2 --> H3[链上铸造/上架]
    H3 --> H4[后端保存 NFT 数据]

    I --> I1[筛选并查看 NFT 详情]
    I1 --> I2{是否购买}
    I2 -- 是 --> I3[链上执行购买]
    I3 --> I4[后端生成订单并更新归属]
    I2 -- 否 --> I5[继续浏览]

    J --> J1[查看持有 NFT]
    J1 --> J2[查看买入/卖出记录]
    J2 --> J3[修改个人资料]

    H4 --> K([结束/返回系统])
    I4 --> K
    I5 --> K
    J3 --> K
```

## 3. 使用建议

1. 如果你要放论文正文，建议优先使用“精简版论文插图流程图”。
2. 如果你要答辩讲解系统闭环，建议使用“系统总体流程图”。
3. 如果你需要，我还可以继续给你画：
   - 钱包登录流程图
   - NFT 创建流程图
   - NFT 购买流程图
   - 后端处理流程图
