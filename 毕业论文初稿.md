# 基于区块链的 NFT 数字藏品市场系统的设计与实现

说明：本文依据 `D:\AStudy\NFT` 项目的源码阅读结果撰写，作为一版可继续加工的本科毕业论文初稿。学校要求的封面、目录、页眉页脚、摘要字数、参考文献格式、致谢与附录等内容，可在此基础上继续按模板调整。

## 摘要

随着数字内容生产与传播方式的持续演进，图片、音频、视频等数字作品的复制成本不断降低，作品的唯一性证明、版权归属确认以及可信流转记录成为数字内容平台面临的重要问题。区块链具备去中心化、不可篡改与可追溯等特征，能够为数字资产确权提供底层支撑；非同质化通证（Non-Fungible Token，NFT）则进一步为数字作品建立了唯一标识与可交易载体。围绕上述问题，本文以一个完整实现的项目为基础，设计并实现了一套基于区块链的 NFT 数字藏品市场系统。

本文系统采用“链上确权与交易、链下业务管理与展示”的协同架构。前端基于 Next.js 与 React 构建，负责页面展示、钱包交互、交易流程反馈与个人资产管理；后端基于 Go、Gorilla Mux、GORM 与 MySQL 实现，负责用户认证、NFT 数据管理、订单管理、头像上传与 IPFS 接口；链上部分基于 Solidity 与 Hardhat 实现 NFTCollection 智能合约，支持铸造、上架、下架、购买与版税结算；分布式存储部分通过 Pinata 接入 IPFS，实现 NFT 媒体文件及元数据的持久化存储。

结合实际源码实现，系统完成了钱包签名登录、多媒体 NFT 创建、IPFS 元数据生成、链上铸造与上架、真实购买、订单同步、市场浏览、详情页交易、个人中心管理与通知提醒等功能。项目一方源码总量约为 7637 行，其中前端 5487 行、后端 1943 行、智能合约 175 行、部署脚本 32 行，说明系统实现已覆盖表示层、服务层、数据层与链上逻辑。源码分析结果表明，该系统已经形成从用户登录到 NFT 创建、展示、交易与资产管理的完整业务闭环。与此同时，系统在链上事件回执校验、自动化测试完备度、生产构建稳定性与部分中文编码一致性方面仍有进一步优化空间。

本文的研究与实现结果表明，采用链上链下分层设计，可以在保障 NFT 资产确权与交易可信性的同时，提高市场查询、用户管理与交互体验的可用性。该系统具备较强的课程设计与毕业设计应用价值，也为后续扩展合约事件监听、推荐系统、后台管理与多链兼容提供了实现基础。

关键词：区块链；NFT；智能合约；IPFS；数字藏品市场

## Abstract

With the rapid growth of digital content creation and distribution, the cost of copying images, audio, and video works has become extremely low, making ownership confirmation, copyright protection, and trustworthy circulation increasingly difficult. Blockchain technology provides decentralization, immutability, and traceability, while Non-Fungible Tokens (NFTs) offer unique identifiers and tradable carriers for digital works. Based on a fully implemented project, this thesis designs and implements an NFT digital collectibles marketplace system based on blockchain technology.

The system adopts a collaborative on-chain and off-chain architecture. The frontend is developed with Next.js and React and is responsible for user interaction, wallet access, market browsing, transaction feedback, and asset management. The backend is implemented with Go, Gorilla Mux, GORM, and MySQL, handling authentication, NFT records, order records, avatar uploading, and IPFS-related services. The smart contract layer is built with Solidity and Hardhat, providing NFT minting, listing, delisting, purchasing, and royalty settlement. Pinata and IPFS are used to store media files and metadata in a decentralized manner.

According to the project source code, the system has implemented wallet-signature login, multimedia NFT creation, IPFS metadata generation, on-chain minting and listing, NFT purchase, order synchronization, marketplace browsing, detail-page trading, profile management, and notification features. The first-party source code contains approximately 7,637 lines, covering the presentation layer, service layer, data layer, and blockchain logic. The implementation demonstrates a complete business workflow from user login to NFT creation, display, trading, and personal asset management. However, there is still room for improvement in event-based on-chain verification, automated testing, production build stability, and text encoding consistency.

The results show that a layered on-chain/off-chain design can preserve the trustworthiness of NFT ownership and transactions while significantly improving usability for data management and market interaction. The system has practical value for graduation design and can serve as a foundation for future extensions such as event listeners, recommendation mechanisms, management dashboards, and multi-chain support.

Key words: blockchain; NFT; smart contract; IPFS; digital collectibles marketplace

## 第一章 绪论

### 1.1 研究背景

在移动互联网和内容平台高度发达的背景下，数字作品的生产门槛不断降低，图片、音乐、视频等内容的创作与传播效率显著提升。然而，传统数字内容的复制成本几乎趋近于零，导致作品的唯一性、归属关系和流转过程难以被可靠记录。在此场景下，创作者常常面临作品被任意复制、转载甚至二次售卖的问题，普通平台数据库也难以提供跨平台、可验证、不可篡改的资产凭证。

区块链技术通过共识机制、密码学签名和分布式账本，为数字资产确权提供了一种新的技术路径。尤其是 NFT 标准提出以后，数字作品不再只是“一个文件”，而可以被映射为链上的唯一 Token，借此建立创作者、拥有者与交易历史之间的可信关联。与同质化代币不同，NFT 更适合承载艺术品、游戏道具、门票、音视频作品与数字证书等具有唯一性或稀缺性的对象。

但是，仅有 NFT 合约并不足以构成完整系统。实际的数字藏品平台还需要考虑用户登录、媒体文件存储、元数据生成、市场检索、交易历史、个人中心与异常提示等问题。也就是说，NFT 技术要真正落地，必须与前后端工程、数据库、文件存储和交互设计共同协作。因此，设计并实现一个具备完整业务闭环的 NFT 数字藏品市场系统，具有较强的研究与实践价值。

### 1.2 研究目的与意义

本文的研究目的，是以区块链确权能力为核心，设计并实现一个面向数字藏品场景的 NFT 市场系统，使用户能够完成钱包登录、作品上传、IPFS 存储、NFT 铸造、链上交易、订单查看和资产管理等完整流程。该系统不以单一演示页面为目标，而是强调从“可创建”到“可交易”再到“可管理”的全链路闭环。

从理论意义上看，本文探索了链上链下协同架构在 NFT 应用中的实现方式。一方面，链上适合承担资产唯一性、交易可信性和版税结算等关键职责；另一方面，链下适合处理高频查询、用户资料、市场筛选和订单聚合展示等业务逻辑。如何平衡两者边界，是 NFT 平台设计的关键问题。本文通过具体实现给出了可操作的方案。

从实践意义上看，该项目形成了可直接演示与继续扩展的系统原型。相较于仅支持图片铸造的简单 Demo，本文系统支持图片、音频和视频三类 NFT，并加入封面图、版税、交易进度展示、通知提醒和个人中心等功能，更接近真实产品的交互方式。对于计算机专业毕业设计而言，该系统能够较好地体现前端开发、后端服务、数据库设计、智能合约和分布式存储的综合应用能力。

### 1.3 本文主要研究内容

围绕上述目标，本文完成的主要工作包括以下五个方面。

1. 设计了链上链下协同的 NFT 数字藏品市场总体架构，明确前端、后端、数据库、IPFS 和智能合约的职责边界。
2. 实现了基于钱包地址、随机 nonce 与数字签名的身份认证机制，并通过 JWT 建立链下会话。
3. 实现了面向图片、音频、视频的 NFT 创建流程，支持文件校验、封面上传、IPFS 媒体存储与元数据自动生成。
4. 编写了支持铸造、上架、购买、下架和版税结算的 ERC-721 智能合约，并通过 Hardhat 提供部署能力。
5. 完成了市场浏览、详情页交易、个人中心、订单记录与通知提醒等前端功能，形成完整业务闭环。

### 1.4 论文结构安排

本文共分为七章。第一章介绍研究背景、意义和主要内容；第二章介绍系统实现涉及的关键技术；第三章对系统需求进行分析；第四章给出系统总体设计与数据库、合约设计；第五章结合项目源码说明关键模块的具体实现；第六章从测试与分析角度总结系统现状与问题；第七章对全文进行总结，并提出后续优化方向。

## 第二章 相关技术基础

### 2.1 区块链与智能合约

区块链本质上是一种按照时间顺序组织的分布式账本。网络中的节点通过密码学机制与共识算法共同维护账本状态，使得链上数据具有难以篡改和可追溯的特征。在 NFT 场景中，区块链记录的不是作品原文件本身，而是与作品对应的资产凭证、所有权变更关系和交易过程。

智能合约是运行在区块链上的程序逻辑。与传统后端业务相比，智能合约具有公开透明、执行结果可验证的特征，但也受到执行成本高、状态更新慢和链上数据存储昂贵等限制。因此，在系统设计中，适合将确权、转移、版税结算等关键业务放在链上，而将市场筛选、资料管理与高频查询等功能保留在链下。

### 2.2 NFT 与 ERC-721 标准

NFT 是指具有唯一属性、不可互换的链上数字资产。以太坊生态中最广泛使用的 NFT 标准是 ERC-721。该标准定义了 `ownerOf`、`safeTransferFrom`、`tokenURI` 等核心接口，用于描述某个 Token 的所有者、转移逻辑以及元数据访问方式。本文系统中的 NFTCollection 合约基于 OpenZeppelin 提供的 ERC721URIStorage 扩展实现，在满足标准接口的基础上增加了上架、购买和版税相关逻辑。

ERC-721 的价值在于，它为 NFT 平台之间的数据互通与钱包识别提供了统一基础。前端页面可以根据 Token ID 和 URI 读取作品信息，钱包和区块浏览器也可以借助标准接口识别资产归属。这使得 NFT 不再依赖单一平台的私有数据结构，而能够作为更通用的数字资产存在。

### 2.3 EIP-2981 版税机制

在数字内容交易场景中，创作者通常希望在二级交易中继续获得一定比例的收益。EIP-2981 提出了 NFT 版税信息标准，允许合约根据销售价格返回版税接收地址和版税金额。本文系统在合约中继承 ERC721Royalty，并为每个 Token 设置可选的版税比例与接收者，从而实现基础的二级销售收益分配能力。

项目中将版税上限设置为 2500 bps，即 25%。当前端创建 NFT 时填写版税参数，合约会在铸造阶段写入版税信息；当用户调用 `buy` 函数时，合约会先计算版税金额，再将剩余金额支付给卖家。该设计兼顾了创作者权益与交易灵活性。

### 2.4 IPFS 分布式存储

链上不适合直接保存图片、音频和视频等大体积媒体文件，因此 NFT 通常采用“链上存 URI，链下存内容”的方式组织数据。IPFS 是典型的内容寻址分布式存储网络，其核心特点在于使用内容标识符（CID）定位资源，而非使用中心化服务器地址。只要文件内容不变，其 CID 就保持稳定，有利于提升数字作品的可验证性和可移植性。

本文系统通过 Pinata 接入 IPFS。后端在接收到用户上传的 NFT 文件后，会先完成媒体类型校验，再上传主文件与封面文件，随后自动生成元数据 JSON，并将元数据再次上传至 IPFS。最终，前端使用 `ipfs://` 形式的元数据 URI 调用合约完成铸造，同时保留网关 URL 供页面展示。这样既满足了链上 `tokenURI` 的需要，也兼顾了浏览器访问的便利性。

### 2.5 前端技术栈

前端采用 Next.js 14 与 React 18 构建。Next.js 的 App Router 结构适合组织首页、市场页、详情页、个人中心等多页面应用；React 的组件化机制则有利于抽离卡片、标签页、通知面板和交易进度条等可复用界面元素。样式层使用 Tailwind CSS，并辅以自定义全局样式实现视觉风格统一。

在 Web3 交互方面，前端通过 ethers.js 调用浏览器注入钱包，兼容 MetaMask、OKX 与 Bitget 等插件。`lib/web3.js` 中封装了钱包检测、签名、铸造、购买、上架、下架、版税读取与价格单位换算等逻辑，有利于降低页面组件的复杂度。

### 2.6 后端与数据库技术

后端采用 Go 1.22 编写，利用 Gorilla Mux 组织 REST 风格路由，使用 GORM 访问 MySQL 数据库。Go 语言在并发处理、部署简洁性和服务性能方面具备优势，适合承担 NFT 市场中的认证、上传、订单和查询等接口服务。MySQL 则用于保存用户、NFT 和订单等链下业务数据。

此外，后端还实现了 JWT 鉴权、中间件式 CORS 处理、简单限流、MIME 类型校验与价格规范化等辅助机制。尤其在价格设计上，系统同时保存 `priceWei` 和 `price` 两个字段：前者以字符串形式保存链上精确数值，后者以浮点数形式用于界面展示。这样可以在保障链上精度的同时兼顾前端可读性。

## 第三章 系统需求分析

### 3.1 角色与业务场景分析

本文系统并未将用户严格切分为互斥角色，而是采用“统一用户，多重行为”的思路。一个完成钱包登录的用户既可以作为创作者发布作品，也可以作为买家购买其他作品，还可以在个人中心查看自己的持有资产和交易历史。因此，系统的核心场景可以概括为四类：身份认证场景、作品创建场景、市场交易场景和资产管理场景。

在身份认证场景中，用户需要通过钱包地址完成身份确认，并在后续访问受保护接口时自动携带令牌。在作品创建场景中，用户需要上传媒体文件、填写名称、描述、分类、价格和版税信息，并触发链上铸造。在市场交易场景中，用户需要浏览在售 NFT、查看详情、完成真实购买、重新上架或下架。在资产管理场景中，用户需要查看当前持有的 NFT、历史买入记录、历史卖出记录以及个人资料。

### 3.2 功能需求分析

结合项目源码，系统的功能需求可归纳为表 3-1。

| 模块 | 主要需求 | 对应实现 |
| --- | --- | --- |
| 用户认证 | 钱包连接、nonce 获取、签名登录、JWT 会话、资料修改 | `auth` 路由、`WalletConnectButton`、`AuthService` |
| NFT 创建 | 输入作品信息、上传图片/音频/视频、可选封面、设置价格与版税、调用链上铸造 | 创建页、IPFS 接口、`mintNFTWithWallet` |
| 市场展示 | 首页概览、市场列表、分类过滤、关键字搜索、排序、收藏 | 首页、市场页、`MarketplaceCard` |
| NFT 交易 | 详情展示、购买、更新价格、重新上架、下架、交易进度显示 | 详情页、`buyNFTWithWallet`、`listNFTWithWallet` |
| 订单管理 | 创建订单、查询买入记录、查询卖出记录、查询某 NFT 历史成交 | `orders` 模块、详情页、个人中心 |
| 资产管理 | 当前持有资产、用户名与头像更新、通知提醒 | 个人中心、上传头像、通知组件 |

从业务闭环看，系统需要满足以下连续性要求：用户在登录后应能够直接进入 NFT 创建流程；创建成功后作品应出现在市场列表和个人中心；购买完成后订单与持有关系应同步更新；交易历史应可在详情页和个人中心中被追踪。

### 3.3 非功能需求分析

#### 3.3.1 安全性需求

系统需要避免仅凭钱包地址冒充用户身份，因此采用随机 nonce 与签名校验方式完成认证。受保护接口应要求 JWT 令牌；NFT 交易金额需要进行精度规范化；媒体文件上传需要校验 MIME 类型与大小；链上购买过程需要具备基础重入防护；高频上传接口需要具有一定限流机制。

#### 3.3.2 一致性需求

系统需要保证链上状态与链下展示尽量一致。NFT 是否处于上架状态、当前所有者是谁、订单是否创建成功，都直接影响用户体验。当前实现中，前端在链上交易确认后主动回传交易结果给后端，以维持业务可用性；后续则需要进一步增强基于事件与回执的链上校验能力。

#### 3.3.3 易用性需求

区块链应用的典型问题是流程长、等待时间不确定。系统应通过进度提示、错误提示、媒体预览、单位换算与通知提醒降低用户理解成本。项目中已实现 IPFS 上传进度、钱包确认提示、链上广播状态、后台同步状态等分阶段反馈，这属于重要的易用性设计。

#### 3.3.4 可扩展性需求

考虑到后续可能扩展更多链、更多 NFT 类别和更复杂的市场逻辑，系统应保持前后端与智能合约的模块边界清晰。本文项目采用前端 `lib` 封装、后端 `handler-service-repository` 分层以及独立 Hardhat 目录的方式，使后续替换合约地址、增加合约事件监听或补充后台管理模块具备较好的可扩展基础。

### 3.4 可行性分析

从技术可行性看，前端、后端、数据库、智能合约与 IPFS 均已有成熟技术生态支持，项目也已在代码层完成主要模块实现。从经济可行性看，系统使用的框架与开发工具均为开源方案，部署阶段可先基于测试网完成联调，不需要高额成本。从操作可行性看，用户只需安装常见浏览器钱包插件即可参与登录和交易，整体使用路径明确。因此，本系统具备较强的综合可行性。

## 第四章 系统总体设计

### 4.1 系统总体架构设计

本系统采用“前端展示层 + 后端服务层 + 数据存储层 + 区块链合约层”的分层架构，如表 4-1 所示。

| 层次 | 核心组成 | 主要职责 |
| --- | --- | --- |
| 展示层 | Next.js 页面、React 组件、ethers.js | 市场展示、钱包交互、交易触发、进度反馈、个人中心 |
| 服务层 | Go、Mux、GORM、JWT | 认证鉴权、NFT 管理、订单管理、上传服务、IPFS 网关封装 |
| 数据层 | MySQL、本地静态目录 | 用户资料、NFT 记录、订单记录、头像文件 |
| 分布式存储层 | Pinata、IPFS | 主文件、封面文件、元数据 JSON 存储 |
| 区块链层 | Solidity、Hardhat、Sepolia | 铸造、上架、购买、版税结算、所有权转移 |

在实际运行中，用户首先通过前端连接钱包并完成签名登录；前端获得 JWT 后，即可调用后端受保护接口。创建 NFT 时，媒体文件先上传到后端，再由后端转发至 Pinata 并生成元数据；前端拿到元数据 URI 后调用合约执行铸造或铸造并上架；交易成功后，前端再将链上结果同步回后端，供市场列表与个人中心查询。购买流程同理，先链上成交，再链下入库订单并更新持有关系。

这种设计的优势在于：关键资产状态由区块链保证可信，业务查询与页面展示由后端和数据库提供高效支持，媒体文件则交由 IPFS 提供相对去中心化的长期存储。

### 4.2 前后端模块设计

#### 4.2.1 后端模块划分

后端模块整体采用 `handler -> service -> repository/model` 的组织方式。`router.go` 负责统一注册路由；`handler` 处理 HTTP 参数解析与响应；`service` 承担核心业务逻辑；`repository` 负责数据库初始化；`model` 描述数据结构；`middleware` 提供 JWT 鉴权、CORS 与限流能力；`util` 则封装价格换算和文件检测等通用逻辑。

从路由定义可见，后端主要提供以下接口组。

1. `auth`：钱包 nonce 获取、钱包登录、资料更新。
2. `nfts`：NFT 列表、详情、创建、上架价格更新。
3. `orders`：创建订单、查询买入和卖出记录、查询 NFT 历史订单。
4. `upload`：头像上传。
5. `ipfs`：NFT 文件与元数据上传。

#### 4.2.2 前端模块划分

前端页面主要包括首页、登录页、注册页、市场页、NFT 创建页、NFT 详情页和个人中心页。页面之外，系统还抽离了市场卡片、分类标签页、交易进度卡片、钱包连接按钮和通知面板等公共组件。前端 `lib/api.js` 对后端接口进行了统一封装，`lib/web3.js` 对链上交互进行了统一封装，`lib/media.js` 则统一处理 IPFS 地址解析和媒体类型识别。

这种页面与能力分离的方式有两个优点：一是页面组件聚焦用户交互流程，二是同一套链上或后端逻辑可以被多个页面复用。例如，购买逻辑既可能被详情页调用，也可能在未来被市场卡片快速购买功能复用。

### 4.3 数据库设计

根据 `models.go`，系统数据库主要包括用户表、NFT 表和订单表。

#### 4.3.1 用户表设计

用户表用于保存链下身份资料，关键字段包括用户编号、邮箱、用户名、头像地址、密码哈希、钱包地址、nonce 与创建时间。虽然系统当前已收口为钱包签名登录，但数据结构仍保留了邮箱字段，为后续账户体系统一提供了空间。钱包地址在数据库中设置为唯一索引，用于保证同一钱包只能对应一个用户。

#### 4.3.2 NFT 表设计

NFT 表是系统的核心业务表，保存链下市场展示所需信息。除了合约地址和 Token ID 之外，还包含名称、描述、图片地址、媒体地址、媒体类型、MIME 类型、TokenURI、MetadataURL、存储方式、分类、拥有者 ID、链上精确价格 `priceWei`、展示价格 `price`、价格单位以及版税接收地址和版税比例等字段。该结构能够同时支撑首页展示、市场筛选、详情页交易和个人中心查询。

#### 4.3.3 订单表设计

订单表用于保存每次购买行为的链下记录，字段包括 NFT ID、买家 ID、卖家 ID、价格、交易哈希、状态和创建时间。与 NFT 表结合后，可以构建个人中心中的买入、卖出历史，也可以在详情页中回溯某个作品的成交记录。

### 4.4 智能合约设计

本文系统的核心合约为 NFTCollection。该合约继承了 ERC721URIStorage、ERC721Royalty、Ownable 和 ReentrancyGuard，说明其在标准 NFT 功能之外，还具备版税扩展、所有权管理与重入保护能力。

合约中定义了 `Listing` 结构体，用于描述某个 Token 当前是否处于上架状态、卖家地址和标价。系统使用 `_listings` 映射保存每个 Token 的上架信息，使用 `_tokenRoyaltyBps` 保存每个 Token 的版税比例。核心函数包括：

1. `safeMint`：仅铸造 NFT，不直接上架。
2. `mintAndList`：铸造 NFT 后立即按给定价格上架。
3. `listToken`：为已持有的 Token 设置上架价格。
4. `cancelListing`：取消上架。
5. `buy`：支付购买并完成所有权转移与版税结算。
6. `getListing`：读取上架状态。
7. `getRoyaltyInfo`：读取版税接收者、版税金额和比例。
8. `totalMinted`：读取累计铸造数量。

在安全设计上，`buy` 函数引入了 `nonReentrant` 修饰符；在业务一致性上，购买前会校验卖家是否仍然拥有该 NFT，以避免出现“链下仍显示在售，但链上所有者已变化”的陈旧上架问题；在版税设计上，若版税接收者与卖家一致，则不重复扣款。

### 4.5 关键业务流程设计

#### 4.5.1 钱包签名登录流程

钱包签名登录流程如下：

1. 用户在前端选择钱包插件并请求授权连接地址。
2. 前端将钱包地址发送给后端 `/auth/wallet/nonce` 接口。
3. 后端生成随机 nonce，并在数据库中写入对应用户记录。
4. 前端构造 `NovaNFT Login\nnonce: xxx` 消息，调用 `personal_sign` 让钱包签名。
5. 前端将签名结果和钱包地址发送至 `/auth/wallet/login`。
6. 后端恢复公钥地址，校验签名合法性及与请求地址是否一致。
7. 校验通过后，后端清空 nonce 并生成有效期为 24 小时的 JWT。
8. 前端将 JWT 与当前用户信息写入本地存储，后续请求自动带上 `Authorization` 头。

#### 4.5.2 NFT 创建流程

NFT 创建流程如下：

1. 用户输入名称、描述、价格、分类、版税信息并上传媒体文件。
2. 前端先进行基础校验，如分类与媒体是否匹配、文件大小是否合法。
3. 前端将主文件、封面文件与文本信息提交给后端 `/ipfs/nft`。
4. 后端识别 MIME 类型，上传主文件与封面到 Pinata，并生成元数据 JSON。
5. 后端返回媒体 CID、元数据 URI、网关地址和媒体类型等信息。
6. 前端根据是否设置价格，选择调用 `safeMint` 或 `mintAndList`。
7. 链上交易确认后，前端获取 Token ID、交易哈希和版税参数。
8. 前端向后端 `/nfts` 接口写入链下 NFT 记录，形成可查询市场数据。

#### 4.5.3 NFT 购买流程

NFT 购买流程如下：

1. 买家进入详情页，读取链下 NFT 信息和链上上架信息。
2. 前端确认当前钱包不是卖家本人，并检查 NFT 是否在售。
3. 买家点击购买，钱包弹出交易确认。
4. 合约执行 `buy`，校验上架状态、金额与所有者状态后完成转账与所有权转移。
5. 前端等待交易回执，并获得交易哈希。
6. 前端调用后端 `/orders` 接口创建订单记录。
7. 后端在事务中写入订单并更新 NFT 拥有者和上架状态。
8. 详情页与个人中心刷新后，即可显示新的持有关系与订单历史。

## 第五章 系统实现

### 5.1 项目结构与源码规模

根据对项目源码的统计，本项目一方源码总量约 7637 行。其中，前端代码约 5487 行，后端代码约 1943 行，智能合约约 175 行，Hardhat 配置与部署脚本约 32 行。该规模表明项目并非单一页面或单文件 Demo，而是由页面层、服务层、数据层与链上逻辑共同组成的系统原型。

从目录结构看，项目主要包含四个一级模块：`frontend`、`backend`、`contracts` 和 `hardhat`。其中 `frontend` 负责用户可见功能，`backend` 负责链下业务服务，`contracts` 保存主合约源码，`hardhat` 用于合约编译和部署。该结构清晰地体现了前后端分离与链上链下协同的总体思路。

### 5.2 身份认证模块实现

后端身份认证实现集中在 `AuthService` 与 `AuthHandler` 中。`GenerateNonce` 会先标准化钱包地址格式，如果数据库中尚无对应用户，则自动创建用户记录并以钱包地址前缀生成默认用户名。随后，服务端为该地址生成 16 字节随机 nonce，并写回数据库。该设计既实现了“首次连接自动建号”，也为后续签名校验准备了随机挑战值。

`WalletLogin` 会将钱包地址与签名结果一并提交，由后端按以下逻辑完成校验：首先检查 nonce 是否存在且未失效，然后按照以太坊消息签名规则对登录消息进行哈希，再使用 `crypto.SigToPub` 恢复公钥，并将恢复出的地址与请求地址进行比较。只有两者完全一致时，才会签发 JWT。由于登录成功后会立即清空 nonce，因此同一 nonce 不能被重复利用。

前端的钱包连接组件 `WalletConnectButton` 进一步增强了可用性。该组件能够自动检测浏览器注入钱包，并对 MetaMask、OKX 和 Bitget 进行适配；在交互上，它将流程拆分为“连接钱包、获取 nonce、等待签名、创建登录会话”四个阶段，并配合弹窗向用户展示当前状态。登录成功后，JWT 与用户信息将被保存到浏览器本地存储，并在后续 API 请求中自动携带。

### 5.3 多媒体上传与 IPFS 元数据实现

与仅支持图片的 NFT 系统不同，本文项目同时支持图片、音频和视频三类媒体。前端创建页会根据用户所选分类限制可上传文件的类型：艺术类仅允许图片，音乐类仅允许音频，视频类仅允许视频，其他类可兼容多种媒体格式。对于音频与视频，系统允许用户额外上传封面图，以提升市场列表和详情页的展示效果。

后端 `ipfs.go` 中对文件大小与媒体类型做了详细限制。NFT 主文件最大为 100MB，封面文件最大为 20MB，头像文件最大为 5MB。`util/upload.go` 使用 `http.DetectContentType` 配合白名单判断实际 MIME 类型，从而降低仅凭文件扩展名伪装上传的风险。对于 NFT 主文件，系统支持常见的 JPG、PNG、GIF、WEBP、MP3、WAV、OGG、FLAC、AAC、MP4、WEBM、MOV 等格式。

在文件上传完成后，后端会根据媒体类型自动生成元数据 JSON。图片类作品直接写入 `image` 字段；音频与视频类作品则写入 `animation_url`，如果存在封面图，则同时补充 `image` 字段。这一设计符合 NFT 元数据的常见使用方式，使钱包和市场前端能够正确识别展示资源。

之后，前端拿到 `metadataUri` 后调用链上合约完成铸造，并在成功后将 NFT 基本信息写回数据库。由此，IPFS 在系统中承担了媒体文件与元数据的存储职责，而数据库则承担了市场展示与查询职责。

### 5.4 智能合约与链上交互实现

合约实现方面，NFTCollection 合约通过 `_tokenIdCounter` 维护自增 Token 编号，通过 `_listings` 保存上架信息，通过 `_configureRoyalty` 统一配置版税参数。对前端而言，最关键的能力是“仅铸造”和“铸造即上架”两种流程同时存在，因此 `safeMint` 与 `mintAndList` 两组接口分别对应不同业务场景。

前端 `lib/web3.js` 中对链上交互做了较完整封装。`mintNFTWithWallet` 会先根据用户输入价格进行单位换算，价格可以以 ETH、WEI、GWEI、BNB、MATIC、USD、CNY 等形式输入，最终统一转换为 ETH 和 Wei 再上链。若价格大于 0，则调用 `mintAndList`；否则调用 `safeMint`。若用户启用了版税，则同时传入版税接收地址与费率。

对于后续管理场景，前端分别封装了 `listNFTWithWallet`、`delistNFTWithWallet` 和 `buyNFTWithWallet`。在购买逻辑中，系统会先读取链上上架信息，并校验当前钱包地址与链上持有者是否满足交易条件，再执行支付购买。购买成功后返回交易哈希、价格信息和链上数据来源，为后端创建订单提供必要参数。

### 5.5 市场展示与资产管理实现

首页承担市场概览作用。页面启动后，会请求所有在售 NFT，并统计当前在售数量、地板价、平均价和热门分类。市场页则在此基础上进一步加入分类过滤、关键字检索、价格区间筛选、多种排序方式和本地收藏夹功能。对于用户而言，这些功能能显著降低在大量 NFT 中查找目标作品的成本。

`MarketplaceCard` 组件根据 NFT 媒体类型自动选择渲染方式：若为视频，则使用自动播放的短视频预览；若为音频，则展示封面图；若为图片，则直接展示图片本身。同时，卡片还会展示分类、Token 编号、价格与版税信息，并带有点击涟漪动画和收藏按钮。这些细节增强了市场页面的产品感和交互完整性。

NFT 详情页是系统中业务最密集的页面之一。该页面不仅展示 NFT 的基础信息和媒体内容，还承担购买、重新上架、更新价格和下架等交易功能。页面会先读取后端 NFT 数据，再尝试读取链上版税信息与链上上架状态，并根据当前用户是否为所有者动态切换操作按钮。对于普通用户，页面提供真实购买能力；对于持有者，页面则提供更新价格与下架操作。

个人中心页实现了“当前持有、历史买入、历史卖出”三类资产视图，并支持用户名修改和头像上传。由于系统中的 NFT、订单和用户资料都已写入数据库，因此个人中心能够通过后端接口直接进行聚合展示。这使整个项目不仅能完成单次交易，还能追踪用户的长期资产状态。

### 5.6 通知与交易体验实现

区块链应用通常存在“等待时间长、交易状态不透明”的问题。为了解决这一问题，系统设计了 `TxProgressCard` 组件，将创建和购买等流程拆分为 IPFS 上传、钱包确认、链上确认、后台同步和完成等多个阶段。只要用户发起创建或购买操作，页面就会持续展示当前所处阶段，从而避免用户因长时间无反馈而误判系统卡死。

此外，系统还实现了通知中心 `NotificationBell`。该组件会周期性调用“历史买入”和“历史卖出”接口，并将新出现的订单转化为本地通知项，支持已读、未读、全部已读和清理已读等功能。虽然该设计本质上仍是前端轮询，而非真正的消息推送，但对于毕业设计阶段的项目而言，已经体现出对用户持续使用场景的考虑。

### 5.7 安全性与鲁棒性设计

系统在安全与鲁棒性方面采取了多项措施。

1. 认证层面采用 nonce 加签名的方式校验钱包持有权，避免仅凭地址冒充身份。
2. JWT 中间件会从 `Authorization` 头中解析用户编号，并注入到请求上下文，供后续写操作使用。
3. 上传层面对头像和 NFT 文件分别设置大小限制，并根据真实内容识别 MIME 类型。
4. 后端为头像上传和 IPFS 上传接口配置了基础限流，降低恶意刷接口风险。
5. 价格处理层统一使用 Wei 字符串作为链上精确值，并通过 `NormalizeWeiString` 和 `ResolveWeiAndDisplay` 避免展示价格与链上价格不一致。
6. 智能合约购买函数使用 `ReentrancyGuard` 保护，并在成交前检查卖家是否仍为当前链上拥有者，防止陈旧上架数据导致异常购买。

总体来看，系统虽然仍偏开发阶段，但已在身份、上传、价格和链上交易关键点上体现出一定的安全意识。

## 第六章 系统测试与结果分析

### 6.1 测试环境与验证方式

由于本项目当前仍处于毕业设计开发阶段，测试工作以源码验证、模块逻辑检查与关键链路联调为主。项目技术环境包括 Go 1.22、Next.js 14、React 18、Hardhat、Solidity 0.8.24、MySQL 与浏览器钱包插件。本文在阅读源码基础上，对系统主流程进行了功能性验证分析，并额外对后端执行了 `go test ./...` 编译检查，结果通过，但当前尚未编写独立单元测试用例。

### 6.2 关键功能验证分析

表 6-1 给出了项目主要功能的当前实现情况。

| 验证项 | 验证内容 | 当前实现情况 |
| --- | --- | --- |
| 钱包登录 | 获取 nonce、钱包签名、恢复地址、签发 JWT | 已实现完整链路 |
| NFT 文件上传 | 主文件上传、封面上传、大小限制、媒体识别 | 已实现完整接口 |
| IPFS 存储 | 媒体上传、元数据生成、CID 与网关地址返回 | 已实现 |
| 铸造与上架 | 调用 `safeMint` 或 `mintAndList`，返回 Token ID 与交易哈希 | 已实现 |
| NFT 购买 | 合约支付购买、所有权转移、订单入库、下架同步 | 已实现 |
| 个人中心 | 查询持有 NFT、买入记录、卖出记录、修改资料 | 已实现 |
| 通知提醒 | 基于订单轮询生成未读通知 | 已实现基础能力 |
| 后端编译检查 | `go test ./...` | 通过，但无测试文件 |

从业务闭环角度看，系统已经具备以下连续验证能力：用户可完成登录；登录后可创建 NFT；创建后的 NFT 可进入市场页与详情页；买家可在详情页购买；购买成功后订单与持有关系可在个人中心体现。就毕业设计中期或原型系统标准而言，这一闭环已经较为完整。

### 6.3 系统现有优势分析

#### 6.3.1 功能闭环完整

当前系统最大的优势在于并非停留于单一的铸造示例，而是已经形成“登录、上传、铸造、上架、购买、订单同步、资产管理”的完整链路。这说明项目具备系统化思维，而非仅对某一技术点进行展示。

#### 6.3.2 支持多媒体 NFT

系统支持图片、音频与视频三类 NFT，并为音频与视频提供封面图处理逻辑。在同类课程项目中，这种多媒体支持可以明显提升项目的完整性和展示效果，也更符合数字内容资产化的实际需求。

#### 6.3.3 兼顾链上可信与链下体验

通过将所有权转移、购买支付与版税结算放在链上，将资料管理、市场筛选和订单查询放在链下，系统较好地平衡了可信性与易用性。该架构既保留了 NFT 的核心价值，也使系统具有更接近真实平台的用户体验。

### 6.4 当前存在的问题

尽管系统已形成主要业务闭环，但从毕业论文和后续工程化角度看，仍存在以下不足。

#### 6.4.1 链上链下同步仍依赖前端回传

当前 NFT 创建与购买完成后，前端会在拿到链上回执后主动将结果提交给后端，以写入数据库。该方案有助于快速打通主流程，但仍然依赖前端作为同步中介。若未来需要进一步提升一致性，应增加基于交易回执或合约事件监听的后端校验机制。

#### 6.4.2 自动化测试不足

后端虽然可以通过编译检查，但当前所有目录均显示“无测试文件”。前端与合约层也尚未看到系统化自动化测试用例。对毕业设计而言，这意味着系统的可用性更多来自人工联调和流程验证，而非测试驱动的稳定性保证。

#### 6.4.3 工程配置仍偏开发态

源码中 CORS 策略较为宽松，部分配置项仍保留开发默认值，通知模块也采用轮询而非推送方式实现。这些设计在开发与展示阶段是可接受的，但在生产化场景中仍需进一步加强安全配置、服务治理与运行效率。

#### 6.4.4 部分中文字符串存在编码不一致现象

在若干后端错误信息和个别前端文案中，可以观察到中文显示异常的情况。这类问题不会直接阻断系统功能，但会影响最终展示质量与论文答辩观感，因此应作为后续优化重点之一。

### 6.5 后续优化方向

结合上述问题，本文认为系统后续可从以下几个方向继续完善。

1. 引入合约事件监听器或后端主动校验交易回执，增强链上链下一致性。
2. 补充后端单元测试、接口测试、前端关键页面测试与合约测试。
3. 优化前端生产构建与部署流程，提升可交付性。
4. 增强账户体系设计，如支持邮箱账户与钱包账户绑定。
5. 将通知方式从轮询升级为 WebSocket 或基于事件驱动的消息推送。
6. 修复编码与文案一致性问题，进一步打磨界面与交互细节。

## 第七章 总结与展望

本文围绕 NFT 数字藏品市场场景，设计并实现了一套基于区块链的完整系统。系统以 NFTCollection 智能合约为链上核心，以 Go 后端为链下服务中枢，以 Next.js 前端为用户交互入口，并借助 Pinata 与 IPFS 解决大体积媒体文件和元数据的存储问题。通过对项目源码的系统性分析，可以确认该系统已经实现了钱包签名登录、多媒体 NFT 创建、IPFS 元数据管理、链上铸造与上架、购买结算、订单同步、市场展示和资产管理等关键能力。

与简单的 ERC-721 铸造示例相比，本文项目更强调“系统化落地”。一方面，链上合约承担确权、上架、购买和版税结算职责；另一方面，链下数据库和服务承担用户资料、订单记录、市场查询和通知提醒职责。该设计不仅符合 NFT 平台的实际需求，也体现了区块链应用开发中“可信状态上链、高频业务下链”的常见工程思路。

当然，本文系统仍处于从“功能可用”迈向“工程成熟”的阶段。当前仍存在自动化测试不足、链上链下同步依赖前端回传、配置偏开发态、少量文案编码不一致等问题。未来可进一步从事件驱动同步、测试体系建设、运维部署优化、多链支持与后台管理等方向展开研究与扩展。

总体而言，本文完成的 NFT 数字藏品市场系统已经具备较高的原型完整度和毕业设计价值。它不仅验证了区块链、IPFS、前后端分离和智能合约协同应用的可行性，也为后续继续深入数字资产平台、Web3 用户身份体系与去中心化内容确权机制的研究提供了实践基础。

## 参考文献

[1] Nakamoto S. Bitcoin: A Peer-to-Peer Electronic Cash System[EB/OL]. [2026-04-18]. https://bitcoin.org/bitcoin.pdf

[2] Buterin V. A Next-Generation Smart Contract and Decentralized Application Platform[EB/OL]. [2026-04-18]. https://ethereum.org/en/whitepaper/

[3] Entriken W, Shirley D, Evans J, et al. ERC-721: Non-Fungible Token Standard[EB/OL]. [2026-04-18]. https://eips.ethereum.org/EIPS/eip-721

[4] Detrio J, Giton M, et al. ERC-2981: NFT Royalty Standard[EB/OL]. [2026-04-18]. https://eips.ethereum.org/EIPS/eip-2981

[5] IPFS Docs. Content Addressing[EB/OL]. [2026-04-18]. https://docs.ipfs.tech/concepts/content-addressing/

[6] OpenZeppelin. ERC721 API and Extensions[EB/OL]. [2026-04-18]. https://docs.openzeppelin.com/contracts/4.x/api/token/ERC721

[7] Vercel. Next.js Documentation[EB/OL]. [2026-04-18]. https://nextjs.org/docs

[8] ethers.js Documentation[EB/OL]. [2026-04-18]. https://docs.ethers.org/v6/

## 附录A 前置页占位符

以下内容为占位符，后续可按学校模板直接替换。

### A.1 封面占位符

学校名称：____________

学院名称：____________

专业名称：____________

论文题目：基于区块链的 NFT 数字藏品市场系统的设计与实现

学生姓名：____________

学号：____________

指导教师：____________

职称：____________

完成日期：____________

### A.2 原创性声明占位符

原创性声明：

本人郑重声明：所呈交的毕业论文（设计）是本人在指导教师指导下独立完成的研究成果。除文中已经注明引用的内容外，本论文不包含任何他人已经发表或撰写过的研究成果。对本文研究作出重要贡献的个人和集体，均已在文中以明确方式标明。本人完全意识到本声明的法律后果由本人承担。

学生签名：____________

日期：____________

指导教师签名：____________

日期：____________

### A.3 致谢占位符

致谢：

【此处填写致谢内容，可从指导教师、同学帮助、项目开发过程收获等角度展开。】

### A.4 目录占位符

目录：

【此处待根据学校 Word 模板自动生成目录。】

## 附录B 系统运行截图占位符

以下图片标题先作为占位符保留，后续可替换为真实截图并按学校格式编号。

图 B-1 系统首页运行截图

【插入首页截图】

图 B-2 NFT 创建页面截图

【插入创建页截图】

图 B-3 NFT 详情页面截图

【插入详情页截图】

图 B-4 个人中心页面截图

【插入个人中心截图】

图 B-5 交易哈希或区块浏览器截图

【插入交易记录或浏览器截图】

## 附录C 图表与流程图占位符

以下图示先保留占位符，后续可以用 Visio、ProcessOn、draw.io 或 Word 自带绘图工具替换。

图 C-1 系统总体架构图

【插入系统总体架构图】

图 C-2 数据库 E-R 图

【插入数据库 E-R 图】

图 C-3 钱包登录流程图

【插入钱包登录流程图】

图 C-4 NFT 创建流程图

【插入 NFT 创建流程图】

图 C-5 NFT 购买流程图

【插入 NFT 购买流程图】

## 附录D 测试表格占位符

以下表格为测试章节占位符，后续可继续补充测试编号、输入条件、截图编号和实际结果。

| 测试编号 | 测试模块 | 测试内容 | 输入/操作 | 预期结果 | 实际结果 | 是否通过 | 截图编号 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| T01 | 用户登录 | 钱包获取 nonce 并完成签名登录 | 连接钱包并点击登录 | 返回 token，进入个人中心 | 【待填写】 | 【待填写】 | 【待填写】 |
| T02 | NFT 创建 | 上传图片并完成铸造 | 填写名称、价格并上传图片 | 成功创建 NFT 并写入市场 | 【待填写】 | 【待填写】 | 【待填写】 |
| T03 | IPFS 上传 | 上传音频/视频与封面 | 选择媒体与封面后提交 | 返回 assetUri 与 metadataUri | 【待填写】 | 【待填写】 | 【待填写】 |
| T04 | 市场浏览 | 分类筛选与搜索 | 输入关键词并切换分类 | 返回符合条件的 NFT 列表 | 【待填写】 | 【待填写】 | 【待填写】 |
| T05 | NFT 购买 | 执行购买并生成订单 | 在详情页点击购买 | 链上成交，订单写入数据库 | 【待填写】 | 【待填写】 | 【待填写】 |
| T06 | 个人中心 | 查看持有与历史订单 | 进入个人中心 | 显示当前持有、买入、卖出记录 | 【待填写】 | 【待填写】 | 【待填写】 |

## 附：后续补充建议

1. 上述附录 A、B、C、D 已经预留占位符，后续可以直接替换为学校模板、截图、流程图和测试结果。
2. 如果导师要求查重率更低，可以继续结合你自己的开发过程，把“需求分析”和“实现细节”章节写得更贴近个人实际工作。
